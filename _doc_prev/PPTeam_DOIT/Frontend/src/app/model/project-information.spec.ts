import { ProjectInformation } from './project-information';

describe('ProjectInformation', () => {
  it('should create an instance', () => {
    expect(new ProjectInformation()).toBeTruthy();
  });
});
