import { UserSubmissionInformation } from './UserSubmissionInformation';

describe('UserSubmissionInformation', () => {
  it('should create an instance', () => {
    expect(new UserSubmissionInformation()).toBeTruthy();
  });
});
